//Escribe una función para calcular el área de un círculo. Puedes utilizar funciones declarativas.

function area_de_un_ciculo(radio) {
    const pi = Math.PI;
    const area = pi * radio * radio;
    return area;
  }
  
  //como debe de funcionar
  const radio = 5;
  const area_de_un_ciculovalor = area_de_un_ciculo(radio);
  console.log(`el area de un circulo es: ${radio} is ${area_de_un_ciculovalor}.`);




