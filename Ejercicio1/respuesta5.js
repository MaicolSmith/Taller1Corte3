//Escribe una función para calcular el área de un círculo. Puedes utilizar funciones declarativas.

function area_de_un_circulo(radio) {
    const pi = Math.PI;
    const area = pi * radio * radio;
    return area;
  }
  
  //
  const radio = 5;
  const area_de_un_circulovalor = area_de_un_circulo(radio);
  console.log(`el area del circulo ${radio} es ${area_de_un_circulovalor}.`);




