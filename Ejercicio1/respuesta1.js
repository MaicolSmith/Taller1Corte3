///Suma de Números:Escribe una función que tome dos números como parámetros y devuelva su suma.
function suma_dos_numeros(num1, num2) {
    return num1 + num2;
  }
  
  const sum = suma_dos_numeros(5, 3);
  console.log(`la suma de los dos numeros es: ${sum}`);

